export const INVALID_NODE_IMPORT = 'Node imports can only exist in .server.{ts,js,tsx,jsx} files';

export const MOVE_TO_SERVER_FILE = 'Move this logic to a .server.ts file and import that file instead.';
